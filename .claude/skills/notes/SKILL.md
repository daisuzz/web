---
name: notes
description: このリポジトリの `/notes/<slug>` （Zettelkasten風の個人Wiki、`src/content/notes/*.md`）に新しいノートを作成・編集するときのワークフロー。`/notes` コマンドで明示的に呼ばれたときはもちろん、「〇〇についてノート書いて」「このURLをノートにして/まとめて」「これノートに残しておいて」のような依頼、あるいはユーザーが技術・プロダクト・概念について素朴なオープンクエスチョンをして回答した直後（ノート化を提案すべきタイミング）にも積極的に使うこと。ノートのfrontmatter形式、タイトルの決め方、`[[wikilink]]`/`#tag`の構文制約、ハブノート(MOC)、Mermaid図、URLをノート化する際の主語の選び方、文体、出典の扱いなど、ノート執筆に関する全ての規約をここに集約している。
---

# notes — 個人Wikiノートの作成・編集

`/notes/<slug>` はユーザー（tokuhirom）の個人用Zettelkasten的ノート機能。`src/content/notes/*.md` にMarkdownファイルを置くだけでページが生成される。このスキルはノートを**実際に書く・編集するときの方針**をまとめたもの。コード側の実装（`loadNotes.ts`/`noteMarkdown.ts`の処理詳細）はリポジトリの `CLAUDE.md` を参照。

## 設計原則

- **1ノート1概念**: 複数の概念を1つのノートに詰め込まない。話が広がったら別ノートに切り出して `[[...]]` で繋ぐ。
- **概念中心**: 「いつ書いたか」ではなく「何についてのノートか」を主語にする。
- **個別事情を持ち込まない**: ノートは調べた技術・概念そのものについての一般的な知識を記録する場。「このリポジトリでの適用」「このサイトでの採用検討」のような特定プロジェクト・環境固有の事情や個人の採用判断は本文に含めない。
- **分類より関連付け**: フォルダによる階層分類は使わない。`[[wikilink]]` と `#tag` による関連付けだけで整理する。
- **積極的なリンク**: 新しいノートを書く・既存ノートを編集するときは、他のノート本文の中でそのトピックに言及している箇所がないか `src/content/notes/` をGrepし、見つかったら `[[...]]` でリンクする。
- **前置き不要**: 個人用メモなので、読者への配慮や前置きは書かない。ラフな文体でよい。
- **事後の構成調整**: 追記したら、見出しの並びや重複がないか、ノート全体を見直す。

## 新しいノートを追加する手順

1. `src/content/notes/<kebab-case>.md` を作成する。
2. frontmatterに `created`（今日の日付）を書く。既存ノートを更新する場合は `updated` を今日の日付に更新する。frontmatterの値はそのまま使われるだけで、git履歴やファイルのmtimeからは推測されない。
3. 本文1行目に `# タイトル`（日本語可）を書く。frontmatterに`title`は持たせない——このタイトルが `[[wikilink]]` の表示名やページの `<h1>` になる。
4. `npm run develop` でローカルにレンダリングを確認する（可能な場合）。
5. 関連する既存ノートへの `[[...]]` リンクを追加する。

```markdown
---
created: 2026-08-18
---

# タイトル

本文...
```

## 相互リンク（`[[wikilink]]`）

- 書式は `[[<ファイル名（slug、拡張子なし）>]]` のみ。
- 存在しないslugを指定すると、build時に警告が出て、ページ上は打ち消し線付きの「リンク切れ」表示になる。新規ノートを作るなら先にリンクを書いてしまってよいが、typoでないか確認すること。
- バックリンク（「🔗 リンクされているノート」）は自動生成されるので、リンクを追加・削除するだけでよく手動メンテは不要。
- コードブロック・インラインコード内の `[[...]]` は展開されない（そのまま表示される）。

## `#tag` の仕様

- タグとして認識されるのは、直前が行頭・空白・開き括弧類のときの `#` のみ（`https://x.com/foo#bar` のようなURL内の `#` やコードブロック内は無視される）。
- 英数字・アンダースコア・ハイフンのみで構成されるタグ（ASCII）は小文字に正規化される。日本語などそれ以外の文字を含むタグは正規化されない。
- `/notes/tags/` にタグクラウドが自動生成されるので手動メンテ不要。タグを付けるだけでよい。

## ハブノート（MOC）

原子ノートとは別に、同じ話題領域に属する複数の原子ノートを見取り図的に束ねる「ハブノート」（Zettelkasten/LYT文脈での MOC: Map of Content）を作ってよい。ハブノート自身は深掘りした内容を書かず、配下ノートを `[[...]]` でリンクし、それぞれの位置づけ・違いを一言でまとめる役割に徹する。

- 同じ分野の原子ノートが3つ前後たまってきたら、ハブノート作成を検討する（例: `markdown-presentation-tools.md` が `slidev.md`/`marp.md`/`deck-k1low.md` を束ねている）。
- 配下ノート側にも「## [[ハブノート名]]の中での位置づけ」のような見出しでハブノートへの参照を書き、双方向にリンクする。
- ハブノート本文には他のタグに加えて `#moc` タグを付ける。`/notes/tags/moc` でハブノート一覧を一望できるようにするため。

## Mermaid図

`` ```mermaid `` フェンスで書くと、該当ノートを開いたときだけクライアント側で動的importして描画される。図で理解が助かる内容（アーキテクチャ、状態遷移など）では積極的に使ってよい。

## URLを渡されたときのノート化方針

技術記事のURLを渡されて「まとめて」「ノートにして」と言われた場合、その記事の性質によってノートの主語を使い分けること。

- **URL自体（その記事・発表・実装）が有益な情報の場合** — 記事のトピックそのものをノートの主語にする（例: 「HAProxyでの耐量子暗号TLS対応」のような具体的な手順・発表内容が主眼の記事）。
- **記事が要素技術の紹介にとどまる場合** — 記事ではなく、紹介されている要素技術そのものをノートの主語にする（例: 「post-quantum cryptography」や「ML-KEM」など、記事はあくまで概念の入り口として使う）。
- 海外ブログの紹介である場合は、地の文で要約・言い換えて済ませず、原文の内容・構成を尊重して書く。自分の考察を足す場合は、原文の要約とは別セクションに分ける。

## 書き方のトーン

- ラフなメモでOK。前置きや読者への配慮は不要（個人用のメモなので）。
- 日本語で書く。
- 検証結果やコマンド例など、具体的な内容を優先する。
- 特定のプロダクト・技術・サービスを批判したり、けなしたりするニュアンスは入れない。合わなかった点を書く場合も事実ベースで淡々と記述する。出典記事が評価的・煽情的な言葉（例:「カルト」）で形容している場合も、それをそのまま見出しや地の文に持ち込まず、背景にある客観的事実だけを抽出して書く。
- 根拠のある出典がない情報は載せない。ウェブ調査した内容を書く場合は、末尾に「出典」セクションを設けて参照元をリンクする。裏取りできなかった情報や記憶・推測だけの内容は書かないか、推測であると明記する。
- 知識を問う質問（聞き覚えのない単語・固有名詞はもちろん、自分の知識だけで答えられそうに見える一般的な質問も含む）に憶測で即答せず、まずWebSearch/WebFetchで裏取りしてからノートを書く。個人ブログなどの二次情報より公式ドキュメント・一次情報源を優先する。

## 手を動かす実験

説明を読むだけでなく、実際にマシン上でコードを書いて動かし、体感してから書くことを積極的に行う。特にネットワーク・カーネル周りのトピックは、動かしてみないと理解が浅くなりがちなので、可能な場合は最小限の実験コードを書いて動かすことを優先的に検討する（例: `/dev/kvm` のioctlを直接叩く最小プログラムをビルド・実行してKVMのVM exitを体感した `kvm-hello-world-experiment.md`）。

- 実験の記録は、対象となる概念のノート（例: `kvm.md`）に混ぜ込まず、`<トピック>-experiment.md` のような独立したノートに切り出し、概念ノート側からリンクする（1ノート1概念の原則）。
- 実験ノートには、目的・使った素材（参照したリポジトリ等）・実際に躓いた点とその対処・実行結果・コードから読み取れることを書く。単なる感想ではなく再現可能な記録にする。
- 実験コードを実行する前に、何を試すか（実行するコマンド・触るデバイスやリソース等）を説明し、必ずユーザーの許可を取ってから実行すること。

## ノート作成後に確認すること

- **深掘り提案**: 書き終えたノートの本文中に「まだ独立ノートになっていないが、深掘りする余地がありそうな概念」（説明の中で触れているだけの関連用語・隣接する概念）が含まれていないか確認する。見つかったら「〇〇についても別ノートとして切り出して調べられそうですが、書きますか？」のように提案する。ノートを分割・切り出した直後も同様に、そこからさらに切り出せそうな概念がないか確認する。
- **既存ノートへの追記提案**: `/notes` コマンド以外の文脈で、ユーザーがオープンクエスチョン（「〇〇ってどういう用途なの？」のような技術・プロダクト・地名などについての素朴な調査依頼）をしてきた場合は、調べて回答した後に、その内容を `/notes/` に書き残すかどうかを聞く。
